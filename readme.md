# Home Lab

## Prerequisites

### Networking

### Ansible Requirements

Ansible requires python:

```
sudo apt install -y python
```

Ansible requires python:

```
sudo apt install -y python
```

Ansible requires python:

```
sudo apt install -y python
```

